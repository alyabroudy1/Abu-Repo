version = 1

cloudstream {
    // name is inferred from project name
    description = "A simple example provider for testing"
    authors = listOf("AbuRepo")
    language = "en"
    status = 1
    tvTypes = listOf("Movie", "TvSeries")
}
